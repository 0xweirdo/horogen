# Local dev — ค่า DB/พอร์ตตรงกับ docker-compose.yml ที่ root
PORT=3001
DATABASE_URL=postgresql://heygen:heygen@localhost:5433/heygen_dev

# Clerk (จำเป็นบน staging/prod — ใส่ผ่าน Doppler เท่านั้น)
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=

# Observability (optional บน local)
SENTRY_DSN=
